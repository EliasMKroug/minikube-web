<div class="container p-4">
    <h2 class="text-center">Listado de Productos</h2>
    <!-- Botón para agregar productos -->
    <div class="d-flex justify-content-end mb-3">
        <a href="/products/add" class="btn btn-success">+ Agregar Producto</a>
    </div>
    <!-- Tabla de productos -->    
    <div class="row">
        <div class="col-md-12">
            {{#if products.length}}
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Producto</th>
                            <th>Tipo</th>
                            <th>Descripción</th>
                            <th>Stock</th>
                            <th>Precio</th>
                            <th>Costo</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each products}}
                            <tr>
                                <td>{{product}}</td>
                                <td>{{type}}</td>
                                <td>{{description}}</td>
                                <td>{{stock}}</td>
                                <td>${{price}}</td>
                                <td>${{cost}}</td>
                                <td>
                                    <a href="/products/edit/{{id}}" class="btn btn-secondary btn-sm">Editar</a>
                                    <a href="/products/delete/{{id}}" class="btn btn-danger btn-sm">Eliminar</a>
                                </td>
                            </tr>
                        {{/each}}
                        <tr>
                            <td colspan="3" class="text-end"><strong>Total</strong></td>
                            <td><strong>{{totalStock}}</strong></td>
                            <td><strong>${{totalPrice}}</strong></td>
                            <td><strong>${{totalCost}}</strong></td>
                            <td></td>
                        </tr>                        
                    </tbody>
                </table>
            {{else}}
                <div class="alert alert-warning text-center" role="alert">
                    No hay productos guardados aún. 
                    <a href="/products/add" class="btn btn-primary btn-sm">Crear uno nuevo</a>
                </div>
            {{/if}}
        </div>
    </div>
</div>

